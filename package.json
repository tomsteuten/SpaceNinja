{
  "name": "spaceninja",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A gentle 3D solar-system explorer for young children. Vertical slice: Earth to the Moon.",
  "scripts": {
    "dev": "vite",
    "host": "vite --host",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview --host",
    "test": "vitest run",
    "narration:generate": "node scripts/generate-narration-kokoro.mjs",
    "narration:generate:openai": "node scripts/generate-narration.mjs"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "@types/three": "^0.185.4",
    "kokoro-js": "^1.2.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  }
}
